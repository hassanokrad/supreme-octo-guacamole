services:
  pulseboard:
    build:
      context: ..
      dockerfile: Dockerfile
    image: pulseboard:latest
    env_file:
      - ../.env
    ports:
      - "8000:8000"
    restart: unless-stopped
